function soma(x, y ){
    return x + y;
}

console.log(soma(3, 4));

const multipilcacao = function (x , y){
    return x * y;
}

console.log(multipilcacao(5, 5));

const dividir = (x, y) => {
    return x / y;
};

console.log(dividir(10, 2));

const objeto = () => ({ nome: 'Brenno', sobrenome:'Cavalcante'});
